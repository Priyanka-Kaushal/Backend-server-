// const { check } = require("express-validator");

// const permissionAddValidator = [
//   check("permission_name", "permission_name is required").trim().not().isEmpty(),
//   check("default")
//     .optional()
//     .isIn(["0", "1", "2"])
//     .withMessage("default must be 0 or 1 or 2"),
// ];

// module.exports = {
//   permissionAddValidator
// };
